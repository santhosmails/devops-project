# Default values for go-web-app-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 3

image:
  repository: santhosmails/devops-go-web-app
  pullPolicy: IfNotPresent
  tag: "10433558794"

service:
  type: ClusterIP
  nodeport: 30007

ingress:
  enabled: true
